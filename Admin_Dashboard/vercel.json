{
    "version": 2,
    "builds": [
      {
        "src": "angular.json",
        "use": "@vercel/static-build",
        "config": { "distDir": "dist/Admin_Dashboard" }
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/" }
    ]
  }
  